# ---------------------------------------------------------------------------
# IFSMHP Frontend — environment template
#
# Copy to frontend/.env. Vite inlines these values into the built bundle, so
# they are PUBLIC. Never put a secret, key or password in this file.
# ---------------------------------------------------------------------------

VITE_API_BASE_URL=http://localhost:5001/api/v1
